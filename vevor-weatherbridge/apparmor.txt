#include <tunables/global>

profile vevor-weatherbridge flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/python>

  # Capabilities required for container operation
  capability,
  file,
  signal (receive) peer=*,

  # Network access for HTTP server (receiving weather data) and MQTT client
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,

  # DNS resolution (for Weather Underground forwarding)
  network inet udp,
  network inet6 udp,

  # S6-Overlay and container init system
  /bin/** ix,
  /usr/bin/** ix,
  /usr/lib/bashio/** ix,
  /etc/s6/** rix,
  /etc/s6-overlay/** rix,
  /run/s6/** rwix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /init rix,
  /var/run/** rwk,
  /var/run/ rw,

  # Python interpreter execution
  /usr/bin/python3* ix,
  /usr/local/bin/python3* ix,
  /usr/lib/python3*/** r,
  /usr/local/lib/python3*/** r,

  # Application directory (read-only for code)
  /app/** r,

  # Startup script
  /run.sh rix,

  # Proc filesystem (Python runtime needs these)
  /proc/*/fd/ r,
  /proc/*/fd/* rw,
  /proc/*/stat r,
  /proc/*/status r,
  /proc/meminfo r,
  /proc/cpuinfo r,

  # Configuration access (Home Assistant options)
  /data/** rw,
  /config/** r,

  # SSL/TLS certificates (for HTTPS/MQTTS connections)
  /etc/ssl/** r,
  /usr/share/ca-certificates/** r,

  # Timezone data
  /usr/share/zoneinfo/** r,

  # Libraries and system files
  /lib/** r,
  /usr/lib/** r,
  /etc/passwd r,
  /etc/group r,
  /etc/hosts r,
  /etc/resolv.conf r,

  # Temporary files (Python may need these)
  /tmp/** rw,
  /var/tmp/** rw,

  # Allow stdout/stderr for logging
  /dev/null rw,
  /dev/tty rw,
  /dev/pts/* rw,
  /dev/urandom r,
  /dev/random r,
}
